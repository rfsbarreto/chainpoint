<%= form_with(model: badge) do |form| %>
  <% if badge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(badge.errors.count, "error") %> prohibited this badge from being saved:</h2>

      <ul>
        <% badge.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :issue_date %>
    <%= form.text_field :issue_date %>
  </div>

  <div class="field">
    <%= form.label :recipient_name %>
    <%= form.text_field :recipient_name %>
  </div>

  <div class="field">
    <%= form.label :badge_uuid %>
    <%= form.text_field :badge_uuid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
